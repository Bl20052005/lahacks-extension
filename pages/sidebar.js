let button = document.querySelectorAll("analysis");

const divs = document.querySelectorAll("div");

// document.querySelector("test").classList.add("fonts");

console.log(divs.length, button.length);
